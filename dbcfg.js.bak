

// Load module
var mysql = require('mysql');
// Initialize pool
var pool      =    mysql.createPool({
    connectionLimit : 10,
    host     : '127.0.0.1',
    user     : 'huadmin',
    password : '0956',
    port     : 3306 ,
    database : 'hujackman_db',
    debug    :  false
});    
module.exports = pool;           